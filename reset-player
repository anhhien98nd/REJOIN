-- 🌸 Reset Character Script (chỉ dành cho acc "tênngườichơi")

local Players = game:GetService("Players")
local p = Players.LocalPlayer

if not p then
    warn("[Reset] ❌ Không tìm thấy LocalPlayer.")
    return
end

if p.Name ~= "tênngườichơi" then
    warn("[Reset] 🚫 Bỏ qua — chỉ acc 'tênngườichơi' mới được reset.")
    return
end

-- ✅ Reset nhân vật an toàn
print("[Reset] 🔁 Đang reset nhân vật cho:", p.Name)

pcall(function()
    p.Character:BreakJoints() -- reset bằng cách phá joints (giống nút “Reset Character” trong menu)
end)

print("[Reset] ✅ Nhân vật của", p.Name, "đã được reset.")
